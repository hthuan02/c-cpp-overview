#include <stdio.h>
#include <stdlib.h>
#include <stdint.h>

int main(int argc, char const *argv[])
{
    uint16_t *ptr = NULL;
    // Epcs kiểu dữ liệu uint16_t
    ptr = (uint16_t*)malloc(10);

    return 0;
}